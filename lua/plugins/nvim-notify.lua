return {
  "rcarriga/nvim-notify",
  opts = {
    background_colour = "#1e2030", -- Moonlight background
    timeout = 3000,
    stages = "slide",              -- or fade / static
    render = "default",
  },
}
